import { NextFunction, Request, Response } from "express";


export const getAllChats = (req:Request,res:Response,next:NextFunction) => {
    try {
        console.log("this is a test")        
    } catch (error) {   
        next(error)
    }
}

export const getChatInfo = (req:Request,res:Response,next:NextFunction) => {
    try {
        console.log("this is a test")        
    } catch (error) {   
        next(error)
    }
}

export const getMessages = (req:Request,res:Response,next:NextFunction) => {
    try {
        console.log("this is a test")        
    } catch (error) {   
        next(error)
    }
}

export const createGroupChat = (req:Request,res:Response,next:NextFunction) => {
    try {
        console.log("this is a test")        
    } catch (error) {   
        next(error)
    }
}

export const createChat = (req:Request,res:Response,next:NextFunction) => {
    try {
        console.log("this is a test")        
    } catch (error) {   
        next(error)
    }
}

export const addMember = (req:Request,res:Response,next:NextFunction) => {
    try {
        console.log("this is a test")        
    } catch (error) {   
        next(error)
    }
}

export const removeMember = (req:Request,res:Response,next:NextFunction) => {
    try {
        console.log("this is a test")        
    } catch (error) {   
        next(error)
    }
}

export const promoteMember = (req:Request,res:Response,next:NextFunction) => {
    try {
        console.log("this is a test")        
    } catch (error) {   
        next(error)
    }
}
